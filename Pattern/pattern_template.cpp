#include <iostream>
using namespace std;

int main()
{
    int length, height;
    cout << "\nEnter length: ";
    cin >> length;
    cout << "\nEnter height: ";
    cin >> height;
    int mr, ml;

    cout << mr << "&" << ml << endl;
    
    cout << endl;
    for (int i = 1; i <= height; i++)
    {
        for (int j = 1; j <= length; j++)
        {

        }
        cout << endl;
    }
    return 0;
}